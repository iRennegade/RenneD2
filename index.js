const axios = require("axios");
const nodos = 0
const prompt = require("prompt-sync")();

(async () => {
  name = prompt("Nombre de usuario de replit : ")
  
  try {
    axios.post("")
  }
})();

// No usar
